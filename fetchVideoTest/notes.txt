--- API set-up ---
- #npm init 
- answer prompts

#npm install --save express 
express is the framework for the backend 
body-parser will help us handle data later !!!!
!!body-parser is depricated 

touch index.js 

- add start up code 
// server/index.js

const express = require("express");

const PORT = process.env.PORT || 3001;

const app = express();

app.listen(PORT, () => {
  console.log(`Server listening on ${PORT}`);
});

- add a "start" script in the package.json
  "scripts": {
    "start": "node index.js",
    "test": "test"
  },

  -if something is already running error, try,
  #sudo lsof -i :3000
  or :3001

  if something is running like a node server, do 
  #sudo kill -9 PID
  where PID is number 

  -console should log what your log statement
  says on the app.listen function with your
  defined constant PORT


  -add api endpoint before the app.listen
  -navigate to localhost:3001 and see JSON messsage


  -- Set Up Endpoints --
  -set up a few endpoints for your app to interact with 


  --- Front End ---
-index.html 
    -buttons for requests
-fetch.js 
    -onclick fetch and render to page 


resources:
fix CORS error
https://medium.com/@dtkatz/3-ways-to-fix-the-cors-error-and-how-access-control-allow-origin-works-d97d55946d9

many ways to solve, try 
#npm install cors 
int the front end

-- go back to backend and install cors
#npm install cors --save
this will enable yu to access the back end
be sure to install this and add code in the backe end 
const cors = require('cors');
app.use(cors());
app.options('*', cors());